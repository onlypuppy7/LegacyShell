{
  "server-services/start-services.js#serverConnected": "A connecting game/client socket's `auth_key` resolved to a `game_servers` row during its `requestConfig` handshake — fires once per connection, right after the existing `yourServer` lookup. Lets a plugin build its own registry of which live sockets belong to which identified instance (see legacyadmin's `services/registry.js`), so admin-style commands can be routed to a specific instance on demand.",
  "server-services/start-services.js#wsDisconnected": "A previously connected socket closed — counterpart to `serverConnected`, for cleaning up whatever a plugin's registry stored keyed by that socket.",
  "server-services/start-services.js#unhandledCommand": "A `cmd` arrived that none of services' own core cases matched — the general escape hatch for plugin-defined commands (set `plugins.cancel = true` so this doesn't also log it as dropped). Most of legacyadmin's services-side commands (file editor, SQL/table editor relay, moderation CRUD, catalog browser, room overview, account login) are handled here.",
  "server-services/start-services.js#servicesOnLoad": "Services process finished its own setup (DB, math, events), before `initTables()` runs.",
  "server-services/start-services.js#initTablesStart": "Start of the DB-seeding pipeline — hook here to force-reinit `items` before defaults are inserted.",
  "server-services/start-services.js#initTablesBefore": "Just before default items are inserted from `server-services/src/items/*.js` — only fires when the `items` table is currently empty (a genuinely fresh database), so it fires at most once, ever. For a plugin's own item pack, hook `initTables` instead (see below) — that one fires on every boot.",
  "server-services/start-services.js#initTables": "Fires once per boot, after the empty-table check either ran or didn't — the reliable hook for a plugin to insert its own items via `ss.recs.insertItems(...)` every time services starts, regardless of whether the table was already seeded.",
  "server-services/start-services.js#initTablesMaps": "After the `maps` table is unconditionally wiped and reseeded from `server-services/src/maps/*.json` (this happens on every boot, no empty-table gate) — plugins call `ss.recs.insertMaps(...)` here for custom map packs.",
  "server-services/start-services.js#initTablesFinish": "End of the whole DB-seeding pipeline (items + maps both done).",
  "server-services/start-services.js#sendServicesInfo": "The periodic `servicesInfo` push (started by a `requestConfig` handshake) is about to be sent to a polling client/game server.",
  "server-services/start-services.js#addEggs": "A player's egg (currency) balance was incremented via the `addEggs` command.",
  "server-services/start-services.js#addKill": "A kill was recorded for a player via the `addKill` command.",
  "server-services/start-services.js#addDeath": "A death was recorded for a player via the `addDeath` command.",
  "server-services/start-services.js#setAnnouncement": "The global `game_announcement` flag was updated.",
  "server-services/start-services.js#servicesInfoGame": "A game server pushed its `servicesInfo` (room/player-count snapshot) to services.",
  "server-services/start-services.js#validateLoginSuccess": "Password-based login succeeded.",
  "server-services/start-services.js#validateLoginFail": "Login failed — three separate call sites share this event name, covering wrong password, nonexistent user, and a database error respectively; check the payload's `error` field for which one.",
  "server-services/start-services.js#validateLoginViaAuthTokenSuccess": "\"Remember me\" token-based silent login succeeded.",
  "server-services/start-services.js#validateRegisterSuccess": "New account registration succeeded.",
  "server-services/start-services.js#validateRegisterFail": "New account registration failed (username taken, validation error, etc — see `error`).",
  "server-services/start-services.js#feedback": "A player submitted in-game feedback (also posted to a Discord webhook if `config.services.feedback` is set).",
  "server-services/start-services.js#saveEquipBeforeWrite": "Just before a player's equipped loadout is written to the DB.",
  "server-services/start-services.js#buyingResult": "A shop purchase attempt resolved.",
  "server-services/start-services.js#redeemResult": "An item/egg code redemption attempt resolved.",
  "server-services/start-services.js#previewResult": "A code \"preview\" (check without redeeming) resolved.",
  "server-services/start-services.js#tokenSuccess": "The VIP/\"nugget\" token unlock flow succeeded.",
  "server-services/src/data_management/recordsManagement.js#insertMaps": "Fired once per map JSON file as `insertMaps()` writes a `maps` row — the low-level counterpart to `initTablesMaps`.",

  "src/shell/bullets.js#bulletHitEffect": "A bullet impact occurred (wall or player) — a visual/effect hook, fires on both client and server.",
  "src/shell/bullets.js#bulletHitAfter": "After hit resolution/damage has been applied for that bullet.",
  "src/shell/catalog.js#setUpShopAvailableBeforeEventLoop": "Inside the weekly shop-rotation algorithm, before seasonal-event item pools are folded into the candidate set.",
  "src/shell/catalog.js#setUpShopAvailableBeforeApply": "Inside the weekly shop-rotation algorithm, before the final tier selection is written to the database.",
  "src/shell/constants.js#constantsFinished": "All shared constants/enums have been computed at module load — signals other shared modules that it's now safe to reference them.",
  "src/shell/events.js#eventsInit": "The seasonal `EventManager` finished loading `defaultEvents`. This same call site fires under either `services:` or `game:` depending on which process is running it.",
  "src/shell/gametypes.js#GameTypesInit": "Before `GameTypes` is deep-merged against `defaultOptions` — the extension point for registering an entirely new gamemode.",
  "src/shell/guns.js#fireEggk47": "The Eggk47 fired — immediately followed by `if (!plugins.cancel) Bullet.fire(...)`, so a listener can fully replace this weapon's projectile behavior by setting `plugins.cancel`.",
  "src/shell/guns.js#fireDozenGauge": "The Dozen Gauge fired — same `plugins.cancel` override pattern as `fireEggk47`.",
  "src/shell/guns.js#fireDozenGaugeBullet": "One pellet of the Dozen Gauge's shotgun spread is about to fire — fires once per pellet, distinct from the single `fireDozenGauge` event for the overall trigger pull.",
  "src/shell/guns.js#fireCSG1": "The CSG1 (Free Ranger) fired — same `plugins.cancel` override pattern as `fireEggk47`.",
  "src/shell/guns.js#fireCluck9mm": "The Cluck9mm fired — same `plugins.cancel` override pattern as `fireEggk47`.",
  "src/shell/guns.js#fireRPEGG": "The RPEGG (Eggsploder) fired — followed by `if (!plugins.cancel) Rocket.fire(...)` rather than `Bullet.fire(...)`, since this weapon fires a rocket projectile.",
  "src/shell/items.js#AllItems": "Before the flat pickup-item array (`AMMO`, `GRENADE`, …) is indexed into `ItemTypes` — push a new pickup item type onto `data.AllItems` here. Fired from inside the exported `initItems()` function, called explicitly once `plugins.loadPlugins('game')` resolves (see `server-game/run-game.js` and `server-game/src/worker.js`) rather than automatically at module load — module-load-time firing was unreliable server-side. Still fires immediately at module load on the client, where no such ordering issue exists.",
  "src/shell/items.js#itemsLoaded": "After pickup items are indexed — `ItemTypes` and each item's `id` are now final. Same `initItems()` timing as `AllItems` above.",
  "src/shell/loading.js#loadMeshesBeforeMaterial": "During mesh loading, before a material is assigned — lets a plugin swap in a different material/texture.",
  "src/shell/permissions.js#permissionsAfterSetup": "End of `PermissionsConstructor`'s constructor, after every built-in command is registered — the extension point for registering new slash commands via `data.this.newCommand(...)`.",
  "src/shell/player.js#updateBefore": "Start of `Player.update()`, before movement/physics for that tick — the main per-player-per-tick hook.",
  "src/shell/player.js#onStandOnBlock": "Per-tick, when a player's feet resolve against a block mesh — used for special surface behavior (e.g. a parkour gamemode).",
  "src/shell/player.js#onStandOnTile": "Per-tick, when a player's feet resolve against a tile mesh — same use case as `onStandOnBlock` for a different mesh category.",
  "src/shell/player.js#canJump": "Before jump eligibility is finalized — a listener can override the boolean.",
  "src/shell/player.js#onPlayerDeath": "A player has died — `firedId` identifies the killer (or is absent/null for an environmental death).",
  "src/shell/general/prepare-babylons.js#prepareBabylonBefore": "Model-build pipeline: register new `.babylon` filenames into `data.baseBabylons` before file paths are resolved.",
  "src/shell/general/prepare-babylons.js#prepareBabylon": "Model-build pipeline: supply the actual file path for a previously-registered `.babylon` filename.",
  "src/shell/general/prepare-babylons.js#prepareBabylonExtra": "Model-build pipeline: reconcile a per-item \"extra\" babylon/skeleton against the base model (e.g. bone-index fixups).",

  "server-game/start-game.js#onLoad": "Game process finished its own init, before connecting the outbound socket to services.",
  "server-game/start-game.js#connectWebSocketMessage": "Any message received on the outbound services websocket.",
  "server-game/start-game.js#requestConfigReceived": "Specifically a `requestConfig` response (maps/items/servers/config sync) was received from services.",
  "server-game/start-game.js#servicesCommand": "Services routed an admin-style command to THIS specific instance (see `services/registry.js`'s `adminRouteToServer`, which targets by our own `auth_key`) down our existing persistent connection. `msg.payload` carries the actual command; a listener replies via a fresh one-off `#wsrequest` call wrapped as `adminRouteToServerResponse`, not back down this connection.",
  "server-game/src/roomManager.js#sendGameInfo": "Before the aggregated per-room player/room-count snapshot is pushed to services.",
  "server-game/src/roomManager.js#beforeJoinRoom": "Right before a join is accepted, before the existing bootedIps/uuid/session/locked checks — set `plugins.cancel = true` to reject it (closes with `Comm.Close.booted`), e.g. legacyadmin's global ban-list check (`game/banCache.js`).",

  "server-game/src/client.js#beforeChat": "Right before a sent chat message is processed at all (before the chatLineCap/censor/mention handling) - set `plugins.cancel = true` to silently drop it, e.g. legacyadmin's mute enforcement (`game/muteCache.js`).",
  "server-game/src/client.js#clientStartUp": "Module load, once, in the game server's main thread.",
  "server-game/src/client.js#clientInit": "A `ClientConstructor` is being built for a newly joining player or bot.",
  "server-game/src/client.js#clientGameJoinedExtraInfos": "Extra join-time params (e.g. a custom join payload) are being processed for this client.",
  "server-game/src/client.js#clientInitEnd": "End of client construction.",
  "server-game/src/client.js#clientUpdateLoadout": "Before a loadout change (class/weapons/color/hat/stamp) is applied to this client.",
  "server-game/src/client.js#clientUpdateLoadoutEnd": "After a loadout change has been applied to this client.",
  "server-game/src/client.js#clientApplyLoadout": "The loadout is being applied to the live `Player` instance.",
  "server-game/src/client.js#clientInstantiatePlayer": "Before the `Player` object is constructed for this client.",
  "server-game/src/client.js#clientInstantiatePlayerEnd": "After the `Player` object has been constructed for this client.",
  "server-game/src/client.js#requestRespawn": "A respawn request from this client is being processed.",
  "server-game/src/client.js#CommCodeSyncEnd": "End of processing an incoming input-sync packet from this client — a natural hook for anti-cheat heuristics that inspect aim-angle jitter between synced states.",
  "server-game/src/client.js#clientPackSync": "Building this client's outbound sync packet — fires once per sync, before the per-state loop below.",
  "server-game/src/client.js#clientPackSyncLoop": "Building this client's outbound sync packet — fires once per buffered state entry being packed.",

  "server-game/src/rooms.js#roomLoading": "Module load, once, inside the room worker thread.",
  "server-game/src/rooms.js#roomInit": "A room is being constructed.",
  "server-game/src/rooms.js#roomSetSS": "The room worker's local `ss` is being seeded with `{maps, items, permissions, config}` from the main thread.",
  "server-game/src/rooms.js#roomInitGameOptions": "`gameOptions` is being initialized for the room.",
  "server-game/src/rooms.js#roomBeforeMapBuild": "Before the map is built — a listener can rewrite `this.minMap` to substitute a custom map.",
  "server-game/src/rooms.js#roomInitEnd": "Room construction finished; the room is ready to accept players.",
  "server-game/src/rooms.js#roomWsMessage": "A raw, not-yet-decoded websocket message arrived for a client in this room (relayed from the main thread) — a plugin can decode it itself with `new Comm.In(data.content)` to react to a custom opcode.",
  "server-game/src/rooms.js#packRoundEnd": "Building the \"round end\" network packet — fires before the packet body is assembled.",
  "server-game/src/rooms.js#packRoundEndEnd": "Building the \"round end\" network packet — fires after the packet body is assembled.",
  "server-game/src/rooms.js#packRoundUpdate": "Building the \"round update\" (timer) network packet — fires before the packet body is assembled.",
  "server-game/src/rooms.js#packRoundUpdateEnd": "Building the \"round update\" (timer) network packet — fires after the packet body is assembled.",
  "server-game/src/rooms.js#resetGamePlayer": "A player's round-scoped state (score, etc.) is being reset.",
  "server-game/src/rooms.js#roomDetailsUpdated": "Room metadata (name, options, etc.) is changing — fires before the change is applied.",
  "server-game/src/rooms.js#roomDetailsUpdatedEnd": "Room metadata (name, options, etc.) has changed — fires after the change is applied.",
  "server-game/src/rooms.js#packUpdateRoomParams": "Building the room-params sync packet sent to clients — fires before the packet body is assembled.",
  "server-game/src/rooms.js#packUpdateRoomParamsEnd": "Building the room-params sync packet sent to clients — fires after the packet body is assembled.",
  "server-game/src/rooms.js#roomUpdate": "Top of the 60Hz simulation tick, before per-player updates run.",
  "server-game/src/rooms.js#playerUpdate": "Per-player, per-tick update within the main tick loop.",
  "server-game/src/rooms.js#playerStateUpdate": "Per-player state-buffer replay/prediction step within the main tick loop.",
  "server-game/src/rooms.js#roomStateUpdate": "End of the per-tick player loop.",
  "server-game/src/rooms.js#roomSync": "The ~10Hz full-state sync is about to run (every `FramesBetweenSyncs` ticks).",
  "server-game/src/rooms.js#dataSyncLoop": "The ~1000ms \"data sync\" loop (less time-critical per-client data) is running.",
  "server-game/src/rooms.js#clientDataSync": "Per-client step within the data-sync loop.",
  "server-game/src/rooms.js#roomDataSync": "End of the data-sync loop.",
  "server-game/src/rooms.js#metaLoop": "The ~2000ms \"meta\" loop (idle-kick, weather triggers, empty-room destroy) is running.",
  "server-game/src/rooms.js#metaLoopClients": "Per-client step within the meta loop.",
  "server-game/src/rooms.js#roomDestroy": "The room is being torn down.",
  "server-game/src/rooms.js#clientSync": "Building the full-state sync packet — fires once, before the per-client loop below.",
  "server-game/src/rooms.js#clientSyncLoop": "Building the full-state sync packet — fires once per client; a plugin can set `plugins.cancel` here to replace the default position data with its own (e.g. occlusion-based visibility filtering).",
  "server-game/src/rooms.js#clientSyncEnd": "Building the full-state sync packet — fires once at the end, after every client has been packed.",
  "server-game/src/rooms.js#joinPlayer": "A player is joining this room.",
  "server-game/src/rooms.js#registerPlayerClient": "The new `ClientConstructor` is registered into the room's player list.",
  "server-game/src/rooms.js#disconnectClient": "A player is leaving/disconnecting — the closest real equivalent to a \"leave\" hook (there is no separate `leavePlayer` event, despite the naming symmetry you might expect with `joinPlayer`).",
  "server-game/src/rooms.js#getPlayerCountLoop": "Computing the room's live player count/roster — fires once per player being counted.",
  "server-game/src/rooms.js#getPlayerCount": "Computing the room's live player count/roster — fires once with the final tally.",
  "server-game/src/rooms.js#getOldestClientLoop": "Finding the longest-connected client (used for game-owner fallback) — fires once per client being checked.",
  "server-game/src/rooms.js#getOldestClient": "Finding the longest-connected client (used for game-owner fallback) — fires once with the result.",
  "server-game/src/rooms.js#packSetGameOwner": "Building the \"game owner\" packet — fires before the packet body is assembled.",
  "server-game/src/rooms.js#packSetGameOwnerFound": "Building the \"game owner\" packet — fires once an owner has actually been found.",
  "server-game/src/rooms.js#setGameOwner": "Game-owner assignment/reassignment is starting (private rooms only have an owner).",
  "server-game/src/rooms.js#setGameOwnerPrivate": "Game-owner assignment is proceeding, specifically for a private room.",
  "server-game/src/rooms.js#setGameOwnerNew": "A new game owner has been chosen.",
  "server-game/src/rooms.js#getRandomSpawn": "A random spawn point is being picked for a player.",
  "server-game/src/rooms.js#getBestSpawn": "The \"furthest from enemies\" best-spawn algorithm is starting for a player.",
  "server-game/src/rooms.js#getBestSpawnLoop": "The \"furthest from enemies\" best-spawn algorithm — fires once per candidate spawn point being scored.",
  "server-game/src/rooms.js#getBestSpawnEnd": "The \"furthest from enemies\" best-spawn algorithm — fires once with the chosen spawn point.",
  "server-game/src/rooms.js#getValidItemSpawns": "Scanning the map for valid item-spawn cells — fires before the scan starts.",
  "server-game/src/rooms.js#getValidItemSpawnsLoop": "Scanning the map for valid item-spawn cells — fires once per cell being checked.",
  "server-game/src/rooms.js#getValidItemSpawnsEnd": "Scanning the map for valid item-spawn cells — fires once the scan is complete.",
  "server-game/src/rooms.js#notify": "A toast/notification is being sent to one client, as part of a room-wide `room.notify(text, timeoutTime)` broadcast.",
  "server-game/src/rooms.js#packChat": "Building a chat-message packet — fires before the packet body is assembled (runs after `censor.js` filtering has already happened).",
  "server-game/src/rooms.js#packChatEnd": "Building a chat-message packet — fires after the packet body is assembled.",
  "server-game/src/rooms.js#packSpawnItemPacket": "Building an item-spawn packet — fires before the packet body is assembled.",
  "server-game/src/rooms.js#packSpawnItemPacketEnd": "Building an item-spawn packet — fires after the packet body is assembled.",
  "server-game/src/rooms.js#packCollectItemPacket": "Building an item-collected packet — fires before the packet body is assembled.",
  "server-game/src/rooms.js#packCollectItemPacketEnd": "Building an item-collected packet — fires after the packet body is assembled.",
  "server-game/src/rooms.js#packAllItems": "Building the full item-state packet sent on join — fires before the per-pool loop below.",
  "server-game/src/rooms.js#packAllItemsLoop": "Building the full item-state packet sent on join — fires once per item being packed.",
  "server-game/src/rooms.js#spawnItems": "The periodic item-spawning algorithm is starting for this pass.",
  "server-game/src/rooms.js#spawnItemsLoop": "The item-spawning algorithm — fires once per item pool being processed.",
  "server-game/src/rooms.js#spawnItemsLoopSpawn": "The item-spawning algorithm — fires immediately before an individual item is actually spawned.",
  "server-game/src/rooms.js#spawnItemsLoopSpawnEnd": "The item-spawning algorithm — fires immediately after an individual item has been spawned.",
  "server-game/src/rooms.js#spawnItemsEnd": "The periodic item-spawning algorithm has finished this pass.",
  "server-game/src/rooms.js#getItemSpawnFromQueue": "Pulling the next queued spawn position — fires before the position is resolved.",
  "server-game/src/rooms.js#getItemSpawnFromQueueEnd": "Pulling the next queued spawn position — fires with the resolved position.",
  "server-game/src/rooms.js#getUnusedPlayerId": "Allocating a free numeric player-id slot — fires before the scan starts.",
  "server-game/src/rooms.js#getUnusedPlayerIdLoop": "Allocating a free numeric player-id slot — fires once per candidate id being checked.",
  "server-game/src/rooms.js#getPreferredTeam": "Team-balancing logic for a joining player — fires before team counts are tallied.",
  "server-game/src/rooms.js#getPreferredTeamEnd": "Team-balancing logic for a joining player — fires with the chosen team.",
  "server-game/src/rooms.js#getPlayerClient": "Resolving a `Player` back to its owning `ClientConstructor`.",
  "server-game/src/rooms.js#packAllPlayers": "Building the full player-roster packet sent on join — fires before the per-client loop below.",
  "server-game/src/rooms.js#packAllPlayersLoop": "Building the full player-roster packet sent on join — fires once per player being packed.",
  "server-game/src/rooms.js#sendToOne": "Unicast send to one client — fires before the target is resolved.",
  "server-game/src/rooms.js#sendToOneFound": "Unicast send to one client — fires once the target client has been found.",
  "server-game/src/rooms.js#sendToOthers": "Broadcast to all clients but one — fires before the per-client loop below.",
  "server-game/src/rooms.js#sendToOthersLoop": "Broadcast to all clients but one — fires once per client being considered.",
  "server-game/src/rooms.js#sendToOthersLoopFound": "Broadcast to all clients but one — fires once for each client that actually receives the packet.",
  "server-game/src/rooms.js#sendToAll": "Broadcast to every client — fires before the per-client loop below; a plugin can set `plugins.cancel` here to replace default fan-out with custom per-client visibility filtering.",
  "server-game/src/rooms.js#sendToAllLoop": "Broadcast to every client — fires once per client being considered.",
  "server-game/src/rooms.js#sendToAllLoopFound": "Broadcast to every client — fires once for each client that actually receives the packet.",
  "server-game/src/rooms.js#roomLoaded": "Module load, once — hands out the room class itself.",
  "server-game/src/worker.js#adminCommand": "Main thread relayed an admin action into THIS specific room worker (e.g. a kick - see `roomManager.js`'s admin dispatch and legacyadmin's `game/moderationWorker.js`/`game/roomBridge.js`). Only meaningful once `ss.room` exists.",

  "server-client/src/client-static/src/shellshock.min.js#startUp": "The in-browser game client script finished its own top-level setup.",
  "server-client/src/client-static/src/shellshock.min.js#afterBullshit": "Fires after an early setup block (the name is kept verbatim from the original game source).",
  "server-client/src/client-static/src/shellshock.min.js#LegacyShellOnMessage": "Every incoming server packet, after opcode decoding, before dispatch — a general-purpose network message hook; `data.input` is a still-positioned `Comm.In` reader, so a plugin can read a custom opcode's payload here.",
  "server-client/src/client-static/src/shellshock.min.js#roundEndSFX": "Round-end sound/UI cue.",
  "server-client/src/client-static/src/shellshock.min.js#playerDeathAnimation": "A player's death animation is playing.",
  "server-client/src/client-static/src/shellshock.min.js#inGameSync1": "First stage of processing an incoming per-player sync entry.",
  "server-client/src/client-static/src/shellshock.min.js#inGameSync2": "Second stage of processing an incoming per-player sync entry (remote players, distance-based detail).",
  "server-client/src/client-static/src/shellshock.min.js#inGameSync3": "Third stage of processing an incoming per-player sync entry.",
  "server-client/src/client-static/src/shellshock.min.js#clearItemButtons": "Shop/loadout UI item-button reset.",
  "server-client/src/client-static/src/shellshock.min.js#encloseRenderPageFunc": "Rendering one catalog item tile in the shop UI.",
  "server-client/src/client-static/src/shellshock.min.js#playerActorBodyMeshCreated": "A player's visual body mesh finished loading.",
  "server-client/src/client-static/src/shellshock.min.js#playerActorShadowRenderList": "The player mesh was registered with the shadow generator.",
  "server-client/src/client-static/src/shellshock.min.js#playerActorRemoveFromPlay": "A player's visual actor is being hidden (died or left).",
  "server-client/src/client-static/src/shellshock.min.js#playerActorRestoreToPlay": "A player's visual actor is being restored (respawned).",
  "server-client/src/client-static/src/shellshock.min.js#bulletActorFired": "A visual bullet actor was fired client-side.",
  "server-client/src/client-static/src/shellshock.min.js#gunActorSetup": "A weapon's visual model finished setup.",
  "server-client/src/client-static/src/shellshock.min.js#onPageLoadedEnd": "The game's HTML page finished loading.",
  "server-client/src/client-static/src/shellshock.min.js#joinGame": "The client is about to send its join request.",
  "server-client/src/client-static/src/shellshock.min.js#onExtraParams": "Extra join params (session, custom map, etc.) are being assembled.",
  "server-client/src/client-static/src/shellshock.min.js#clientOnExtraGameInfo": "Extra per-game info received from the server after joining.",
  "server-client/src/client-static/src/shellshock.min.js#onShadowGeneratorCreated": "The Babylon shadow generator was created for the scene.",
  "server-client/src/client-static/src/shellshock.min.js#loadSounds": "Before the Apollo/Howler sound list is loaded.",
  "server-client/src/client-static/src/shellshock.min.js#createMapCellsMapLoaded": "The raw map JSON has been fetched and is about to be turned into cells.",
  "server-client/src/client-static/src/shellshock.min.js#modifyMinMap": "A direct hook to rewrite the map data itself before cell generation.",
  "server-client/src/client-static/src/shellshock.min.js#createMapCells": "Map cells are being generated from `minMap`.",
  "server-client/src/client-static/src/shellshock.min.js#onMapCompleteBeforeSetup": "Before final map setup finishes.",
  "server-client/src/client-static/src/shellshock.min.js#onMapComplete": "After final map setup finishes.",
  "server-client/src/client-static/src/shellshock.min.js#loadMaterials": "Core's built-in materials have just been defined - lets a plugin define its own right after (e.g. legacyweather's wetMaterial).",
  "server-client/src/client-static/src/shellshock.min.js#roomParamsUpdated": "A Comm.Code.updateRoomParams payload was just parsed (cheatsEnabled/Object.assign already handled by core) - lets a plugin render its own gameOptions.plugins-owned fields (e.g. legacyweather's weather/time rendering).",
  "server-client/src/client-static/src/shellshock.min.js#resetGamePlayer": "Client-local counterpart to the server's `resetGamePlayer` — round-scoped UI/state reset.",
  "server-client/src/client-static/src/shellshock.min.js#addKillTextBefore": "Before the kill-feed text is rendered.",
  "server-client/src/client-static/src/shellshock.min.js#onBalanceUpdated": "The player's currency balance display changed.",
  "server-client/src/client-static/src/shellshock.min.js#onResourcesLoadedEnd": "All game resources (models/sounds/textures) finished loading.",
  "server-client/src/client-static/src/shellshock.min.js#shellFragBurstBefore": "Before a shell-casing particle burst effect plays.",
  "server-client/src/client-static/src/shellshock.min.js#syncToServer": "The client is about to push a loadout change to the server.",
  "server-client/src/client-static/src/shellshock.min.js#waitForSetupAndAuthComplete": "The startup poll loop, waiting for auth and page setup to both finish, has started.",
  "server-client/src/client-static/src/shellshock.min.js#waitForSetupAndAuthCompleteCheck": "The startup poll loop is checking whether auth and page setup have both finished yet.",
  "server-client/src/client-static/src/shellshock.min.js#waitForSetupAndAuthCompleteReady": "The startup poll loop has confirmed both auth and page setup are ready.",
  "server-client/src/client-static/src/shellshock.min.js#setupComplete": "The startup poll loop concluded successfully.",
  "server-client/src/client-static/src/shellshock.min.js#reachedEnd": "End of the whole client script file, once.",
  "server-client/src/client-static/editor/js/mapEdit.js#loadMeshesBeforeMaterial": "Same hook as the shared `loading.js` version, duplicated for the standalone map-editor page.",

  "server-client/start-client.js#onLoad": "Client process finished its own init, before connecting to services.",
  "server-client/start-client.js#onStartServer": "The Express app instance was just created — the hook plugins use to mount `express.static(...)` for their own assets.",
  "server-client/start-client.js#onRequest": "Global middleware fired on every incoming HTTP request; a listener can set `plugins.cancel` to skip the default `next()`.",
  "server-client/start-client.js#closedBeforeDefault": "Before the \"closed\" maintenance-mode route setup runs.",
  "server-client/start-client.js#closedAfterDefault": "After the \"closed\" maintenance-mode route setup runs.",
  "server-client/start-client.js#openBeforeDefault": "Before the \"open\" mode static-file route setup runs.",
  "server-client/start-client.js#openAfterDefault": "After the \"open\" mode static-file route setup runs.",
  "server-client/start-client.js#onServerRunning": "The Express server is now listening.",
  "server-client/start-client.js#onCheckPassword": "HTTP Basic Auth check, when `config.client.login.enabled`.",
  "server-client/start-client.js#filepaths": "The list of static file paths to serve is being assembled.",
  "server-client/start-client.js#onConnectWebSocket": "Before (re)connecting the outbound websocket to services.",
  "server-client/start-client.js#onMsg": "Any message received on the services websocket.",
  "server-client/start-client.js#onConfigInfoReceived": "Specifically a `requestConfig` response was received.",
  "server-client/start-client.js#onLoadThing": "Per-item within the config load (an item/map/server entry).",
  "server-client/start-client.js#loadingThings": "The batch load of cached config things is running.",
  "server-client/start-client.js#onConfigInfoLoaded": "Config load fully processed and applied to `ss`.",
  "server-client/start-client.js#onServicesRestart": "Services reported a newer `startTime` — the client is about to self-restart via `process.exit(1337)`.",
  "server-client/start-client.js#servicesCommand": "Services routed an admin-style command to THIS specific instance (see `services/registry.js`'s `adminRouteToServer`, which targets by our own `auth_key`) down our existing persistent connection. `msg.payload` carries the actual command; a listener replies via a fresh one-off `#wsrequest` call wrapped as `adminRouteToServerResponse`, not back down this connection.",

  "server-client/src/prepare-modified.js#serversJs": "The `servers.js` template (server list + ports) has been generated.",
  "server-client/src/prepare-modified.js#hashes": "Cache-busting file hashes computed — this fires twice per build (once early, once again after the final build pass), both sharing the same event name.",
  "server-client/src/prepare-modified.js#pluginSourceInsertion": "The main hook for shipping plugin JS into the browser bundle — push `{filepath, insertBefore, insertAfter, position}` onto `pluginInsertion.files`.",
  "server-client/src/prepare-modified.js#doReplacements": "The token-replacement pass that inlines `#hashtag` shared-source files into the bundle — fires once overall, before the per-entry loop below.",
  "server-client/src/prepare-modified.js#doReplacementsLoop": "The token-replacement pass — fires once per replacement entry being applied.",
  "server-client/src/prepare-modified.js#replacementsBefore": "Before the `LEGACYSHELLXXX` → `#hashtag` replacement table is applied — plugins can add their own entries here.",
  "server-client/src/prepare-modified.js#minificationBefore": "Before the UglifyJS minification step; set `plugins.cancel` here to fully replace minification with a different tool/pipeline entirely.",
  "server-client/src/prepare-modified.js#minificationAfter": "After the UglifyJS minification step has run.",
  "server-client/src/prepare-modified.js#minificationSkipped": "Minification was skipped (`config.client.minify` is falsy).",
  "server-client/src/prepare-modified.js#replacementsAfter": "The second replacement pass that inlines final `items`/`maps` JSON and the Babylon.js library source.",
  "server-client/src/prepare-modified.js#htmlContent": "The generated `index.html` content, just before it's written to disk.",

  "server-client/src/stampsGenerator.js#stampImageDirs": "Before scanning for stamp source images — plugins push their own image directories here.",
  "server-client/src/stampsGenerator.js#stampsPreparedSkip": "The stamp spritesheet was already up to date and regeneration was skipped (hash match).",
  "server-client/src/stampsGenerator.js#stampsPrepared": "The stamp spritesheet was (re)generated."
}
